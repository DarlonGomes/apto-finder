# Collector + migrations. Any Postgres works; docker-compose.yml provides one.
DATABASE_URL=postgres://apto:apto@localhost:5432/apto
# Optional, web build: display names for Access emails (default: local part, capitalized).
# VITE_PEOPLE=you@example.com=You,partner@example.com=Partner
